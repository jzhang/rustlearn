

fn add_one(x: i32) -> i32 {
    x + 1
}

fn main() {
    /*
    let x = 5;
    let x = x + 1;
    let x = x * 2;
    println!("The value of x is: {}", x);
    
    let x: (i32, f64, u8) = (500, 6.4, 1);

    let one = x.1;

    println!("one {} , {}", one, add_one(5))
    */
    let a = [1, 2, 3, 4, 5];
    //let index = 10;
    let index = 3;
    let element = a[index];

    println!("The value of element is: {}", element);
}